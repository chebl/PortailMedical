<div id="up_add_user" style="display: none"> <!-- Messages are shown when a link with these attributes are clicked: href="#messages" rel="modal"  -->
	<span style="font-size: medium">Créer un groupe</span>
        <div class="clear"></div>			
                              <form action="{{path('group_create')}}" method="post" {{ form_enctype(formgroup) }}>
                                    
					
					
                                            
					    {{ form_row(formgroup.name, { 'label' : 'Nom' }) }}
                                            
                                            {% for child in formgroup.roles %}
                                               <p>{{ form_label(child) }} {{ form_widget(child) }}</p>
                                            {% endfor %}
					
                                            
					
                                        
					<fieldset>
						<input class="button" type="submit" value="Créer" />
						
					</fieldset>
					
				</form>
				
</div> 